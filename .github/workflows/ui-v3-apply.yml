name: Apply Wasl UI v3 patch

on:
  push:
    branches:
      - agent/ui-redesign-v0.3-corrective
    paths:
      - '.github/workflows/ui-v3-apply.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ui-redesign-v0.3-corrective
          fetch-depth: 0
      - name: Apply corrective UI patch
        run: python3 .github/scripts/ui_v3_patch.py
      - name: Commit result and remove temporary automation
        run: |
          git rm .github/workflows/ui-v3-apply.yml .github/scripts/ui_v3_patch.py
          git config user.name "wasl-ui-bot"
          git config user.email "actions@users.noreply.github.com"
          git add app/src/main/java/com/wasl/app/WaslApp.kt app/src/androidTest/java/com/wasl/app/TodayUiInstrumentedTest.kt
          git commit -m "feat(ui): rebuild home and account entry to approved design"
          git push origin HEAD:agent/ui-redesign-v0.3-corrective
