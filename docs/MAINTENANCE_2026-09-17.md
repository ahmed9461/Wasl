# صيانة 2026-09-17 — الأيقونة وسلامة إدخال المبالغ

## التحقيق

قاعدة المراجعة main: `5aa482257bfd0a72771d34be77d039db316cf77e`.

- ملف `drawable-nodpi/wasl_launcher_art.png` (blob `545fd28796126c462d697483b42c02b159a3019b`) ليس PNG صالحًا؛ بدايته `bf f9 ed 80` لا تطابق توقيع PNG، وفشل فكّه عبر Pillow. الموارد الملونة تشير إليه، بينما الأحادي والإشعارات يشيران إلى رسم تقريبي مختلف.
- مصدر الحروف المعتمد هو أيقونة لوحة التصميم السابقة؛ تتبعت حدودها من مرجع نقطي، وأعيد رسم القوس والزخارف هندسيًا. لا يُدّعى وجود ملف تصميم متجهي أصلي أو تطابق كل بكسل مع اللوحة.
- إعادة إنتاج محلية قبل الإصلاح: `125.50` في الإدخال الذكي أصبحت 12500 وحدة صغرى SAR بدل 12550؛ `١٬٢٣٤٫٥٠` أصبحت 100 بدل 123450؛ وLong.MAX_VALUE بعملة SAR رمى ArithmeticException.
- نموذج المبلغ قبل الإصلاح كان يحذف فواصل غير صحيحة: `1,2.50` أصبحت 12.50 و`1 2` أصبحت 12 و`١٢٬٣٤` أصبحت 1234. الصيغة العلمية كانت مقبولة رغم أنها ليست مدخلًا عشريًا عاديًا.

## التغييرات

- موارد ملونة وأحادية متجهية بهندسة واحدة، بخلفية ممتدة ودائرة أمان 66dp داخل لوحة 108dp؛ legacy/round/adaptive وموارد الإشعارات متسقة.
- MoneyInputParser يتحقق من التجميع والأرقام والإشارة والكسور قبل BigDecimal، بطول أقصى 64 ودون صيغة علمية. تستمر الفاصلة العشرية اللاتينية غير الملتبسة والأرقام العربية والفارسية والتحويل الدقيق إلى Long.
- NaturalEntryParser يمرر كامل الرمز الرقمي إلى المحلل المركزي، ويعيد مبلغًا ناقصًا وتحذيرًا عند رفضه. وجود رموز رقمية متعددة (بما فيها تاريخ رقمي) يحتاج مراجعة بدل تخمين. هذا المحلل المحلي محدود وليس نموذجًا لغويًا عامًا.
- versionCode 2، versionName 0.1.1. لا تعديل في البيانات أو Schema v11 أو Ledger أو الصلاحيات أو التوقيع.

## التحقق

- 42 دالة من اختبارات الدومين وNaturalEntryParser والأيقونة نجحت عبر kotlinc وkotlin-test في بيئة الصيانة. مصدر الاختبارات لم يعدل لتجاوز JUnit؛ فقط النسخ المحلية المؤقتة أزيلت منها تعليقات Test لتشغيلها بحاضنة انعكاسية.
- إضافة عشرة اختبارات انحدار: 3 للدومين، 4 للإدخال الذكي، 3 لموارد الأيقونة، تشمل حالات متعددة داخل كل اختبار.
- فحص مرئي محلي لرسم الشعار الناتج، واختبار حدود جميع منحنيات الرسم داخل دائرة الأمان.
- تعذر تشغيل Gradle محليًا لعدم حل services.gradle.org. البوابة المطلوبة في GitHub: `:core:domain:test :app:testDebugUnitTest :app:lintDebug :app:assembleDebug` والتحقق من Schema v11. النتيجة الملزمة هي Checks للرأس الجاري وmain بعد الدمج؛ نجاح قديم لا يثبت هذا الإصلاح.
- لا تجربة جهاز أو محاكي في هذه الجولة، ولا ادعاء بمراجعة كل مسار للنسخ الاحتياطي أو التذكيرات أو خلو التطبيق من كل الأخطاء.

## ما لم يتغير

لا حذف حسابات أو تغيير أرصدة أو إعادة كتابة دفعات. لم تعدل قواعد البيانات أو الأسرار أو طريقة توقيع النسخ. حافظت CI على عدم استخدام cache دائم وعلى رفع آخر APK في main فقط واحتفاظ يومين. تحديث التطبيق المثبت يحتاج نفس مفتاح التوقيع؛ لا ينصح بإلغاء التثبيت لتجاوز اختلافه.
