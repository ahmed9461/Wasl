name: Apply settings v3 patch

on:
  push:
    branches:
      - agent/ui-redesign-v0.3-corrective
    paths:
      - '.github/workflows/settings-v3-apply.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ui-redesign-v0.3-corrective
          fetch-depth: 0
      - name: Apply settings patch
        run: python3 .github/scripts/settings_v3_patch.py
      - name: Commit result and remove temporary automation
        run: |
          git rm .github/workflows/settings-v3-apply.yml .github/scripts/settings_v3_patch.py
          git config user.name "wasl-ui-bot"
          git config user.email "actions@users.noreply.github.com"
          git add app/src/main/java/com/wasl/app/SettingsHubScreen.kt
          git commit -m "feat(ui): compact settings to approved design"
          git push origin HEAD:agent/ui-redesign-v0.3-corrective
