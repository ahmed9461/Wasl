# قواعد العمل الدائمة في وَصل

هذا المستودع هو مصدر الحقيقة الرسمي لمنتج وَصل. لا تتعامل معه كتجربة مؤقتة.

## ترتيب القراءة الإجباري

قبل أي تعديل:

1. اقرأ WASL_MASTER_PROJECT_PROMPT.md كاملًا.
2. اقرأ PROJECT_CONTEXT.md.
3. اقرأ SPEC.md.
4. اقرأ DECISIONS.md.
5. اقرأ HANDOFF.md.
6. افحص Git status وGit history والملفات المرتبطة بالمهمة.
7. حدد أثر التغيير والاختبارات المطلوبة قبل الكتابة.

## أولوية المصادر

1. أحدث تعليمات صريحة من مالك المشروع.
2. WASL_MASTER_PROJECT_PROMPT.md.
3. SPEC.md والقرارات المعتمدة في DECISIONS.md.
4. التنفيذ الحالي.
5. الافتراض المحافظ الموثق عند بقاء غموض غير مانع.

لا تلغِ متطلبًا قديمًا لأنه لم يُذكر في الطلب الجديد. عند تغير قرار، حدّث الوثائق المتعارضة في المهمة نفسها.

## ثوابت مالية لا يجوز كسرها

- أصل الدين لا يتغير بسبب دفعة جزئية أو كاملة.
- السجل المالي يُضاف إليه ولا يُكتب فوق تاريخه.
- تصحيح دفعة مالية يكون بعكس موثق ثم عملية صحيحة، لا بحذف صامت.
- كل مبلغ يحمل عملته صراحة.
- لا تستخدم Float أو Double للأموال.
- لا تجمع عملات مختلفة في إجمالي واحد دون تحويل صريح ومصدر سعر معروف.
- كل الشاشات والتقارير والمستندات تستمد الرصيد من منطق مالي مركزي واحد.
- العمليات المركبة يجب أن تصبح ذرية عند ربط قاعدة البيانات.

## أسلوب التنفيذ

- اتبع: العرض الظاهر ← التحقيق ← السبب الجذري ← الحل الصحيح ← اختبار الانحدار.
- لا تستخدم ترقيعات دائمة أو تأخيرات عشوائية أو Catch فارغًا.
- لا تكرر Business Logic في UI أو PDF أو التقارير.
- ابدأ بأبسط بنية صحيحة قابلة للاختبار، ولا تقسّم المشروع إلى وحدات بلا حاجة.
- استخدم Kotlin بأسماء واضحة وحالة صريحة ومكونات صغيرة متماسكة.
- أي تغيير في Schema يحتاج Migration واختبار Migration قبل الدمج.
- راجع RTL وLTR والشاشات الصغيرة والكبيرة والوضع الداكن وإمكانية الوصول لكل تغيير مرئي.

## الأمان والخصوصية

- التطبيق Local-first ولا يُضاف Backend إجباري.
- لا تسجل مبالغ أو أسماء أشخاص أو بيانات مالية أو أسرار في Logs.
- لا تضع Tokens أو كلمات مرور أو Keystores أو ملفات env داخل Git.
- أبقِ Android components غير مصدّرة افتراضيًا، ولا تصدّر مكونًا دون سبب واختبار.
- مشاركة الملفات تكون عبر Content URI وFileProvider عند التنفيذ، وليس file URI.
- النسخ الاحتياطية الحساسة يجب أن تكون مشفرة ومتحققًا من سلامتها.

## أوامر التحقق الحالية

- منطق الدومين: ./gradlew :core:domain:test
- اختبارات Android المحلية: ./gradlew :app:testDebugUnitTest
- Lint: ./gradlew :app:lintDebug
- بناء Debug: ./gradlew :app:assembleDebug
- اختبارات Room على جهاز/محاكي: ./gradlew :app:connectedDebugAndroidTest
- التحقق الكامل: ./gradlew :core:domain:test :app:testDebugUnitTest :app:lintDebug :app:assembleDebug

إذا تعذر تشغيل أمر بسبب بيئة العمل، اذكر ذلك صراحة ولا تدّع نجاحه. يعمل GitHub Actions كحاجز تحقق إضافي، لا كبديل عن فهم الفشل.

## قبل الالتزام

- افحص Git status.
- راجع Git diff كاملًا.
- تأكد أن التغيير محدد النطاق ولا يضم ملفات مستخدم غير مرتبطة.
- شغّل الاختبارات المناسبة.
- حدّث SPEC عند تغير السلوك.
- حدّث DECISIONS عند تغير قرار معماري.
- حدّث CHANGELOG للتغييرات المهمة.
- حدّث HANDOFF ليصف الواقع والاختبارات والخطوة التالية.

## تعريف الاكتمال

لا تعتبر المهمة مكتملة إلا بعد تنفيذ المتطلب، والتحقق المناسب، ومراجعة الأمان والفشل والاستمرارية، وتحديث الوثائق، وإجراء مراجعة مستقلة للفرق. اذكر أي جزء غير متحقق أو خطر متبقٍ بوضوح.
