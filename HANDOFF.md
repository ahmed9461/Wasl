# HANDOFF — الحالة الحية

آخر تحديث: 2026-08-25

## الحالة الحالية

- المشروع: **وَصل — Wasl**
- الشعار: **كل حساب له وصل**
- المرحلة: MVP نشط مع الأساس المالي، الاستحقاقات، البحث، الإيصالات، والوعود بالسداد.
- الإصدار: `0.1.0-dev`
- الفرع النشط: `agent/bootstrap-wasl-foundation`
- Pull Request: `#1` إلى `main`، وما زال **Draft**.
- قاعدة البيانات: Room Schema **v5** مع Migrations متسلسلة من v1 إلى v5 دون destructive migration.
- آخر رأس وظيفي اجتاز البوابة الكاملة قبل تحديث التوثيق: `665c7f17b1324e3e7fc190ad4705ff60ea4139c7`.
- آخر تحقق كامل: **Android CI #181** — نجاح كامل.

## ما يعمل الآن

### الحسابات والدفتر المالي

- إنشاء شخص جديد مع دينه الأول.
- إنشاء ديون مستقلة لشخص موجود دون تكرار Person.
- الاتجاهان: **لي عند الناس** و**عليّ للناس**.
- العملات الحالية: YER / SAR / USD.
- تمثيل الأموال بأعداد صحيحة دقيقة حسب Minor Units؛ لا `Float` ولا `Double` في المنطق المالي.
- Ledger append-only هو مصدر الحقيقة المالي.
- تسجيل دفعات جزئية أو نهائية.
- عكس دفعة كحدث مستقل بسبب موثق دون حذف الدفعة الأصلية.
- اشتقاق الرصيد والحالة و`closed_at` من Replay، وليس من تعديل يدوي للرصيد.
- Idempotency للأوامر الحساسة وإعادة المحاولة بعد النتائج غير المؤكدة.

### الاستحقاق والتذكيرات

- تاريخ استحقاق اختياري عند إنشاء الدين.
- تعديل أو إلغاء تاريخ الاستحقاق لاحقًا مع Audit before/after.
- تذكير DUE_DATE عبر WorkManager كـUnique Work.
- Recovery عند تشغيل التطبيق أو تغير الوقت/المنطقة الزمنية.
- معالجة حالات إذن الإشعارات والفشل وإعادة الجدولة دون تكرار العمل.
- شاشة **اليوم** تعرض الديون المتأخرة والمستحقة اليوم.

### البحث والتنقل

- Navigation 3 بين الرئيسية واليوم والبحث وتفاصيل الحساب.
- بحث Room Reactive باسم الشخص أو بيان الدين.
- فتح أي حساب مباشرة من الرئيسية أو Today أو البحث، مع الحفاظ على سياق الرجوع.
- دعم حدود نتائج واضحة ومنع الاستعلام المفتوح غير المحدود.

### إيصالات السداد PDF

- إصدار إيصال سداد PDF عربي لدفعة محفوظة.
- هوية مستند قابلة للتخصيص تشمل الاسم والنشاط والهاتف والتذييل.
- رقم مستند وتسلسل سنوي محفوظان في Room.
- Snapshot ثابت للمستند حتى لا يتغير الإيصال إذا تغيرت البيانات لاحقًا.
- حفظ Metadata وSHA-256 وعدد الصفحات وحالة التوليد.
- إعادة فتح ومشاركة الإيصال من تفاصيل الحساب.
- التحقق من وجود الملف ومن تطابق SHA-256 قبل فتح الإيصال الجاهز.
- CI يستخرج PDF حقيقيًا من التطبيق ويفحصه عبر `pdfinfo` و`pdftotext` و`pdftoppm`.

### وعود السداد

- الوعود بالسداد ككيان مستقل عن الدين وLedger.
- الحالات: `PENDING`, `KEPT`, `MISSED`, `CANCELLED`.
- إنشاء أكثر من وعد للحساب مع الاحتفاظ بالتاريخ الكامل.
- حسم الوعد إلى «تم الوفاء» أو «لم يُنفذ» أو «ملغي» مع ملاحظة اختيارية.
- إنشاء وعد جديد على دين مسدد بالكامل مرفوض، بينما يظل حسم وعد تاريخي موجود مسموحًا للحفاظ على السجل.
- إنشاء الوعد وحسمه Idempotent عبر command IDs مستقلة.
- تفاصيل الحساب تعرض قسم الوعود منفصلًا بوضوح عن سجل العمليات المالية.
- شاشة Today تعرض **وعود اليوم** و**الوعود المتأخرة** وتفتح الحساب المقصود لحسمها.
- يوجد Index مخصص للاستعلام عن الوعود القابلة للمتابعة حسب الحالة والتاريخ.

## قواعد ثابتة لا تكسر

1. Ledger مالي append-only؛ لا حذف أو تعديل حدث مالي سابق لتصحيح التاريخ.
2. Promise ليس عملية مالية ولا ينشئ Payment تلقائيًا.
3. تغيير حالة Promise إلى `KEPT` لا يغيّر الرصيد.
4. Promise لا يغيّر `due_date`، و`due_date` لا يستبدل Promise.
5. كل دين لشخص موجود يبقى حسابًا مستقلًا بمعرف مستقل.
6. الأموال لا تستخدم Floating Point.
7. لا destructive migrations.
8. لا أسرار أو مفاتيح توقيع داخل المستودع.
9. لا دمج إلى `main` دون طلب صريح من صاحب المشروع.

## قاعدة البيانات الحالية — Schema v5

الجداول الأساسية:

- `persons`
- `debts`
- `ledger_entries`
- `reminders`
- `audit_events`
- `document_identities`
- `issued_documents`
- `payment_promises`

Migrations:

- v1 → v2: reminders.
- v2 → v3: audit events.
- v3 → v4: document identities + issued documents.
- v4 → v5: payment promises + indexes المطلوبة للاستعلامات الحالية.

## التحقق الحالي

Android CI #181 على الرأس الوظيفي `665c7f1` نجح بالكامل:

- Unit tests ✅
- Lint ✅
- Debug APK ✅
- Room Schema v5 export/current check ✅
- Android Emulator instrumentation: **44/44** ✅
- استخراج إيصال PDF من ملفات التطبيق ✅
- التحقق أن الإيصال متعدد الصفحات ✅
- التحقق من النص `PAY-2026-00042` ✅
- التحقق من `AL NOOR TRADING` ✅
- التحقق من `123,456.78 USD` ✅
- تحويل صفحات PDF إلى PNG كدليل بصري آلي ✅

Artifacts من #181 تشمل:

- Debug APK.
- Room Schema v5.
- Payment receipt PDF evidence.
- Android instrumentation reports.

## غير منفذ بعد

- الأقساط / Payment plans كميزة مستقلة مكتملة.
- التذكيرات المتكررة العامة.
- المنبه القوي/AlarmManager إن اعتمد لاحقًا ضمن تجربة المنتج.
- قفل التطبيق والحماية المحلية الإضافية.
- Backup/Restore مشفر.
- أنواع مستندات مالية إضافية غير إيصال السداد الحالي.
- البحث المتقدم داخل العمليات والمستندات والمبالغ والتواريخ.
- اختبار بصري يدوي موسع على أحجام نوافذ متعددة وFont Scale مرتفع وTalkBack.

## الخطوة التالية

بعد إغلاق توثيق هذه الجولة والتحقق من CI على رأس التوثيق، تُستأنف الأولويات التالية من `WASL_MASTER_PROJECT_PROMPT.md` و`SPEC.md` دون إسقاط أي قرار سابق. المرشح الوظيفي التالي هو بناء **الأقساط / خطة السداد** فوق نفس قواعد الدقة والتاريخ وعدم خلطها بوعود السداد.
