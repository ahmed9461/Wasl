# HANDOFF — الحالة الحية

آخر تحديث: 2026-08-25

## الحالة الحالية

- المشروع: **وَصل — Wasl**
- الشعار: **كل حساب له وصل**
- الإصدار: `0.1.0-dev`
- الفرع النشط: `agent/bootstrap-wasl-foundation`
- Pull Request: `#1` إلى `main`، وما زال **Draft**.
- قاعدة البيانات: Room Schema **v7** مع Migrations متسلسلة من v1 إلى v7 دون destructive migration.
- هوية Schema v7: `d2c9fe45f2707138bc1476639617e255`.
- آخر رأس وظيفي معتمد قبل تحديث التوثيق: `62c591cb47d014a76353348c80f611d19d611971`.
- آخر تحقق وظيفي كامل: **Android CI #374** — Run `32888565016` — نجاح كامل.

## ما يعمل الآن

### الحسابات والدفتر المالي

- إنشاء شخص جديد مع دينه الأول، أو دين مستقل لشخص محفوظ دون تكرار Person.
- الاتجاهان: **لي عند الناس** و**عليّ للناس**.
- العملات الحالية: YER / SAR / USD.
- الأموال محفوظة كـInteger Minor Units؛ لا `Float` ولا `Double` في المنطق المالي.
- Ledger append-only هو مصدر الحقيقة المالي.
- دفعات جزئية ونهائية وعكس دفعة كحدث مستقل بسبب موثق دون حذف الأصل.
- الرصيد والحالة و`closed_at` مشتقة من Replay، وليست حقولًا مالية تعدل يدويًا.
- Idempotency للأوامر المالية الحساسة وإعادة المحاولة بعد النتائج غير المؤكدة.

### الاستحقاق والمتابعة الذكية

- تاريخ استحقاق اختياري عند إنشاء الدين، مع تعديل أو إلغاء لاحق وAudit before/after.
- المتابعة الذكية تحفظ سلسلة متابعة مرتبطة بالاستحقاق: **قبل الموعد بيوم، يوم الموعد، بعد يومين، ثم أسبوعيًا حتى السداد**.
- المتابعة المحلية تعتمد WorkManager وUnique Work مع Recovery وإعادة جدولة Idempotent.
- تغير الوقت أو المنطقة الزمنية يعيد تقييم الجدولة دون إنشاء أعمال مكررة.
- حالات `BLOCKED_PERMISSION` و`FAILED` قابلة للاسترداد وإعادة الجدولة دون تغيير أصل الدين أو Ledger.
- فتح إشعار المتابعة يقود إلى الحساب المقصود.

### المنبّه القوي / Exact Alarm

- يمكن إضافة **منبّه قوي** اختياري إلى جانب المتابعة الذكية، قرابة 09:00 في يوم الاستحقاق.
- المنبّه القوي لا يستبدل المتابعة الذكية؛ WorkManager يبقى fallback آمنًا إذا تعذر Exact Alarm.
- لا تفتح إعدادات النظام تلقائيًا بمجرد تشغيل السويتش.
- عند نقص الصلاحية يظهر تحذير داخل وَصل وزر صريح **«السماح بالمنبه الدقيق»**؛ الانتقال لإعدادات Android يحدث فقط بفعل المستخدم.
- اختبارات الجدولة تغطي Recovery وتغير المنطقة الزمنية والحفاظ على معنى الساعة المحلية، كما تمنع تسجيل حالة نجاح زائفة قبل توفر الصلاحية.

### وعود السداد

- Promise كيان مستقل عن Ledger وعن `due_date`.
- الحالات: `PENDING`, `KEPT`, `MISSED`, `CANCELLED`.
- تاريخ كامل للوعود وحسم Idempotent دون الكتابة فوق السجل السابق.
- `KEPT` لا تنشئ دفعة تلقائيًا ولا تغير الرصيد.
- تفاصيل الحساب تعرض الوعود منفصلة عن Timeline المالي.
- Today تعرض وعود اليوم والوعود المتأخرة وتفتح الحساب المقصود.

### الأقساط / خطط السداد

- خطط الأقساط مستقلة عن Ledger؛ الخطة تنظّم الجدول ولا تنشئ رصيدًا ماليًا موازيًا.
- إنشاء خطة أقساط ومراجعتها بأوامر Idempotent.
- كل Revision جديدة تحفظ السابقة كـ`SUPERSEDED` بدل تعديل التاريخ أو حذفه.
- الخطة النشطة `ACTIVE` فقط هي التي تقود العرض والمتابعة الحالية.
- كل قسط يحفظ المبلغ المجدول وتاريخ الاستحقاق وتسلسله، بينما `paidAmount` و`remainingAmount` يتصالحان مع العمليات المالية الحقيقية.
- الثابت: `paidAmount + remainingAmount == scheduledAmount`.
- الدفعة الحقيقية تبقى Ledger event.
- مركز **الأقساط** متاح، وشاشة **اليوم** تعرض الأقساط المتأخرة وأقساط اليوم وتستبعد المستقبلية والمدفوعة بالكامل.
- Production يستخدم `InstallmentAwareWaslRepository` دون تكرار منطق Room أو Global state.

### البحث والتنقل

- Navigation 3 للرئيسية واليوم والبحث وتفاصيل الحساب ومركز الأقساط ومراكز المستندات/الإعدادات المضافة.
- بحث Room Reactive باسم الشخص أو بيان الدين مع حد نتائج واضح.
- فتح الحساب مباشرة من الرئيسية أو Today أو البحث أو مركز الأقساط.

### المستندات المالية PDF

- `PAYMENT_RECEIPT`: إيصال سداد عربي لدفعة محفوظة من Snapshot ثابت.
- `DEBT_RECEIPT`: إيصال دين مستقل يمكن إصداره دون اشتراط Ledger entry بعينه.
- `ACCOUNT_STATEMENT`: كشف حساب متعدد الصفحات مبني من Snapshot ثابت يتضمن تاريخ العمليات ومراجعها.
- هوية مستند قابلة للتخصيص ورقم مستند وتسلسل سنوي محفوظان في Room.
- Metadata وSHA-256 وعدد الصفحات وحالة التوليد محفوظة.
- إعادة الفتح والمشاركة لا تتعامل مع مستند `READY` كصالح قبل التحقق من الملف وبصمة SHA-256.
- جدول `issued_documents` هو السجل العام للمستندات، بينما View `payment_issued_documents` يبقى مخصصًا لإيصالات السداد.
- CI يستخرج ملفات PDF حقيقية من التطبيق ويفحصها عبر `pdfinfo` و`pdftotext` و`pdftoppm`.

### النسخ الاحتياطي والاستعادة

- يوجد Backup تطبيقي يدوي مشفر بكلمة مرور، ولا يعتمد على Android Auto Backup.
- النسخة تشمل بيانات جداول وَصل وملفات PDF المرتبطة بسجلات المستندات الجاهزة `READY`.
- قبل إنشاء النسخة يتم التحقق من وجود ملفات المستندات ومن SHA-256 الخاص بها.
- الاستعادة تقبل فقط Schema المدعوم حاليًا، وتتحقق من شكل البيانات والجداول والملفات والمسارات والبصمات.
- ملفات المستندات تُجهّز أولًا في مساحة مرحلية، والبيانات تُختبر في قاعدة Room مؤقتة قبل استبدال الحالة الحية.
- الاستعادة تتحقق من Foreign Keys ومن ثوابت البيانات المالية الأساسية، وتملك مسار Rollback للملفات عند فشل الاستبدال.
- المسارات المطلقة أو الخارجة عن مجلد المستندات، والملفات غير المتوقعة، والبصمات غير المطابقة تُرفض.
- Android Auto Backup وDevice Transfer ما زالا معطلين؛ مسار النسخ المدعوم هو المسار التطبيقي الصريح الذي يختاره المستخدم.

## قواعد ثابتة لا تكسر

1. Ledger مالي append-only؛ لا حذف أو تعديل حدث مالي سابق لتصحيح التاريخ.
2. Promise ليست عملية مالية ولا تغيّر `due_date` ولا الرصيد.
3. Installment Plan ليست Ledger ولا تنشئ رصيدًا موازيًا.
4. السداد الفعلي للأقساط يأتي من Ledger، وتقدم الأقساط مشتق/متصالح معه.
5. Revision جديدة لخطة الأقساط تحتفظ بالخطة السابقة كـ`SUPERSEDED` ولا تمحو التاريخ.
6. المتابعة الذكية والمنبّه القوي أدوات جدولة فقط؛ لا تغير الرصيد ولا تنشئ دفعة.
7. Exact Alarm اختياري؛ WorkManager يبقى fallback.
8. كل دين لشخص موجود يبقى حسابًا مستقلًا بمعرف مستقل.
9. الأموال لا تستخدم Floating Point.
10. المستند الصادر يعتمد Snapshot ثابتًا؛ لا يعاد تفسير محتواه التاريخي من الحالة الحية لاحقًا.
11. مستند `READY` لا يفتح أو يشارك إذا فقد ملفه أو فشل تحقق SHA-256.
12. Backup/Restore لا يتجاوز فحوص Schema والمسارات والبصمات وForeign Keys وثوابت البيانات.
13. لا destructive migrations.
14. لا أسرار أو مفاتيح توقيع داخل المستودع.
15. لا دمج إلى `main` دون طلب صريح من صاحب المشروع.

## قاعدة البيانات الحالية — Schema v7

الجداول الأساسية الحالية تشمل:

- `persons`
- `debts`
- `ledger_entries`
- `reminders`
- `audit_events`
- `document_identities`
- `issued_documents`
- `payment_promises`
- `installment_plans`
- `installments`

والـView الحالي:

- `payment_issued_documents`: يعرض فقط `PAYMENT_RECEIPT` من `issued_documents`.

Migrations:

- v1 → v2: reminders.
- v2 → v3: audit events.
- v3 → v4: document identities + issued documents.
- v4 → v5: payment promises + indexes.
- v5 → v6: installment plans/installments وتاريخ Revisions.
- v6 → v7: إعادة بناء `issued_documents` بحيث يصبح `ledger_entry_id` nullable لدعم مستندات الحساب غير المرتبطة بحركة واحدة، مع حفظ البيانات وإعادة إنشاء الفهارس والـView الخاص بإيصالات السداد.

ملف Schema الحالي: `app/schemas/com.wasl.app.data.local.WaslDatabase/7.json`.

هوية Schema v7: `d2c9fe45f2707138bc1476639617e255`.

اختبارات Migration/Baseline تغطي الانتقال إلى v7، بما فيها انتقال v6→v7 مع الحفاظ على إيصالات السداد السابقة والسماح بالمستندات التي لا تملك `ledger_entry_id`.

## التحقق الحالي

**Android CI #374** — Run `32888565016` — على الرأس `62c591c` نجح بالكامل:

- Unit tests ✅
- Lint ✅
- Debug APK ✅
- Room Schema v7 export/current check ✅
- Android Emulator instrumentation: **59/59** ✅
- 0 skipped / 0 failed ✅
- `BUILD SUCCESSFUL in 4m 36s` لاختبارات Android ✅
- Migration v6→v7 وBaseline migrations ✅
- اختبارات Backup/Restore ونسخ المستندات ✅
- استخراج إيصال السداد PDF من التطبيق وفحصه ✅
- إيصال السداد: صفحتان على الأقل، ويحتوي `PAY-2026-00042` و`AL NOOR TRADING` و`123,456.78 USD` ✅
- استخراج إيصال الدين وفحص `DEBT-2026-00043` و`AL NOOR TRADING` ✅
- استخراج كشف الحساب؛ ثلاث صفحات في عينة CI، وفحص `STAT-2026-00044` و`REF-35` ✅
- تحويل صفحات المستندات إلى PNG كدليل بصري آلي ✅

Artifacts من #374:

- `Wasl-debug` — Artifact `9578696169` — SHA-256 `98316c8d45995c5da560378aa81b2ce5c973104eb9242a6aaf340d625bd47895`.
- `Wasl-room-schema` — Artifact `9578696602` — SHA-256 `4e2c4a81f957bd3462c30362d732b47940da3f5614a5800de20d63befa1bd33f`.
- `Wasl-payment-receipt-evidence` — Artifact `9578927270` — SHA-256 `98f00c4c8cb8b915b192b59a2bb7e017c488cd3902a87097e0ebc3f441d903fc`.
- `Wasl-account-document-evidence` — Artifact `9578927732` — SHA-256 `7d7002e858b182c17efa05fbaf07faa51c1e5e867f806d2c1522c6f60696e6ec`.
- `Wasl-room-instrumentation-results` — Artifact `9578928168` — SHA-256 `c67612ecea115f9089f266601303d9cc9771d71356f1222bc4d3618cb6130716`.

## ديون تقنية غير حاجبة

- نقل Compose tests من `createComposeRule` v1 إلى API v2 بعد ضبط التزامن المطلوب.
- ترقية `actions/setup-java@v4` إلى v5 وتحديث Actions التي ما زالت تصدر تحذيرات Node 20/Node 24 compatibility.
- إزالة safe calls غير الضرورية في `StrongAlarmStoreInstrumentedTest`.
- إزالة `else` الزائد في `AccountDocumentPdfRenderer` بعد أن أصبح `when` exhaustive.
- تحسين زمن CI عبر Gradle configuration cache متى كان ذلك آمنًا.
- بعض رسائل Emulator مثل initial adb offline/ColorBuffer قد تظهر أثناء الإقلاع، لكنها لم تؤثر على نجاح 59/59.

## مرشحات المراحل التالية

تُختار المرحلة التالية من `WASL_MASTER_PROJECT_PROMPT.md` و`SPEC.md` دون إسقاط أي قرار سابق. من المرشحات المتبقية:

- التذكيرات المتكررة العامة خارج سياق الاستحقاق.
- قفل التطبيق/الحماية المحلية والقياسات الحيوية إن اعتمدت.
- بحث متقدم داخل العمليات والمستندات والمبالغ والتواريخ.
- تحسينات إضافية لإدارة المستندات والنسخ الاحتياطية عند ظهور متطلبات فعلية.
- اختبار بصري وإتاحة موسع على أحجام نوافذ وFont Scale وTalkBack.
- تجهيز Release signing للإنتاج عندما يحين وقت الإصدار.
