# HANDOFF — الحالة الحية

آخر تحديث: 2026-08-25

## الحالة الحالية

- المشروع: **وَصل — Wasl**
- الشعار: **كل حساب له وصل**
- الإصدار: `0.1.0-dev`
- الفرع النشط: `agent/bootstrap-wasl-foundation`
- Pull Request: `#1` إلى `main`، وما زال **Draft**.
- قاعدة البيانات: Room Schema **v6** مع Migrations متسلسلة من v1 إلى v6 دون destructive migration.
- هوية Schema v6: `ea8a5a8df2311a0842d64bc34ef91454`.
- آخر رأس وظيفي معتمد قبل تحديث التوثيق: `c92857c70d8e84ae0152dd06f624f3e7b04bed29`.
- آخر تحقق وظيفي كامل: **Android CI #281** — نجاح كامل.

## ما يعمل الآن

### الحسابات والدفتر المالي

- إنشاء شخص جديد مع دينه الأول، أو دين مستقل لشخص محفوظ دون تكرار Person.
- الاتجاهان: **لي عند الناس** و**عليّ للناس**.
- العملات الحالية: YER / SAR / USD.
- الأموال محفوظة كـInteger Minor Units؛ لا `Float` ولا `Double` في المنطق المالي.
- Ledger append-only هو مصدر الحقيقة المالي.
- دفعات جزئية ونهائية وعكس دفعة كحدث مستقل بسبب موثق دون حذف الأصل.
- الرصيد والحالة و`closed_at` مشتقة من Replay، وليست حقولًا مالية تعدل يدويًا.
- Idempotency للأوامر المالية الحساسة وإعادة المحاولة بعد النتائج غير المؤكدة.

### الاستحقاق والمتابعة الذكية

- تاريخ استحقاق اختياري عند إنشاء الدين، مع تعديل أو إلغاء لاحق وAudit before/after.
- المتابعة الذكية تحفظ سلسلة متابعة مرتبطة بالاستحقاق بدل إشعار وحيد فقط.
- التسلسل المعروض للمستخدم: **قبل الموعد بيوم، يوم الموعد، بعد يومين، ثم أسبوعيًا حتى السداد**.
- المتابعة المحلية تعتمد WorkManager وUnique Work مع Recovery وإعادة جدولة Idempotent.
- تغير الوقت أو المنطقة الزمنية يعيد تقييم الجدولة دون إنشاء أعمال مكررة.
- حالات `BLOCKED_PERMISSION` و`FAILED` قابلة للاسترداد وإعادة الجدولة دون تغيير أصل الدين أو Ledger.
- فتح إشعار المتابعة يقود إلى الحساب المقصود.

### المنبّه القوي / Exact Alarm

- يمكن إضافة **منبّه قوي** اختياري إلى جانب المتابعة الذكية، قرابة 09:00 في يوم الاستحقاق.
- المنبّه القوي لا يستبدل المتابعة الذكية؛ المتابعة تبقى fallback آمنًا إذا تعذر Exact Alarm.
- على Android الذي يتطلب صلاحية «المنبهات والتذكيرات»، لا يفتح وَصل إعدادات النظام تلقائيًا بمجرد تشغيل السويتش.
- عند نقص الصلاحية يظهر تحذير داخل نافذة وَصل وزر صريح **«السماح بالمنبه الدقيق»**؛ الانتقال لإعدادات Android يحدث فقط عند ضغط المستخدم على هذا الزر.
- اختبارات الجدولة تغطي Recovery وتغير المنطقة الزمنية والحفاظ على معنى الساعة المحلية للمنبه القوي، كما تمنع تسجيل حالة جدولة ناجحة زائفة قبل توفر الصلاحية.

### وعود السداد

- Promise كيان مستقل عن Ledger وعن `due_date`.
- الحالات: `PENDING`, `KEPT`, `MISSED`, `CANCELLED`.
- تاريخ كامل للوعود وحسم Idempotent دون الكتابة فوق السجل السابق.
- `KEPT` لا تنشئ دفعة تلقائيًا ولا تغير الرصيد.
- تفاصيل الحساب تعرض الوعود منفصلة عن Timeline المالي.
- Today تعرض وعود اليوم والوعود المتأخرة وتفتح الحساب المقصود.

### الأقساط / خطط السداد

- خطط أقساط مستقلة عن Ledger؛ الخطة تنظّم الجدول ولا تنشئ رصيدًا ماليًا موازيًا.
- إنشاء خطة أقساط وتعديلها بأوامر Idempotent.
- كل خطة تملك Revision، والخطة السابقة تتحول إلى `SUPERSEDED` بدل حذف تاريخها.
- الخطة النشطة `ACTIVE` فقط هي التي تقود العرض والمتابعة الحالية.
- كل قسط يحفظ المبلغ المجدول وتاريخ الاستحقاق وتسلسله، بينما `paidAmount` و`remainingAmount` يتصالحان مع العمليات المالية الحقيقية.
- القاعدة الثابتة لكل قسط: `paidAmount + remainingAmount == scheduledAmount`.
- الدفعة الحقيقية تبقى Ledger event؛ لا يوجد «سداد قسط» يغير رصيدًا منفصلًا خارج Ledger.
- مركز **الأقساط** متاح لعرض الحسابات والخطط وإنشاء/تعديل الخطة.
- شاشة **اليوم** تعرض الأقساط المتأخرة وأقساط اليوم وتستبعد المستقبلية والمدفوعة بالكامل.
- Production يستخدم `InstallmentAwareWaslRepository` لتجميع `WaslRepository` و`InstallmentPlanStore` دون تكرار منطق Room أو Global state.

### البحث والتنقل

- Navigation 3 للرئيسية واليوم والبحث وتفاصيل الحساب ومركز الأقساط.
- بحث Room Reactive باسم الشخص أو بيان الدين مع حد نتائج واضح.
- فتح الحساب مباشرة من الرئيسية أو Today أو البحث أو مركز الأقساط.

### إيصالات السداد PDF

- إيصال سداد PDF عربي لدفعة محفوظة من Snapshot ثابت.
- هوية مستند قابلة للتخصيص ورقم مستند وتسلسل سنوي محفوظان في Room.
- Metadata وSHA-256 وعدد الصفحات وحالة التوليد محفوظة.
- إعادة فتح ومشاركة الإيصال بعد التحقق من الملف وSHA-256.
- CI يستخرج PDF حقيقيًا من التطبيق ويفحصه عبر `pdfinfo` و`pdftotext` و`pdftoppm`.

## قواعد ثابتة لا تكسر

1. Ledger مالي append-only؛ لا حذف أو تعديل حدث مالي سابق لتصحيح التاريخ.
2. Promise ليست عملية مالية ولا تغيّر `due_date` ولا الرصيد.
3. Installment Plan ليست Ledger ولا تنشئ رصيدًا موازيًا.
4. السداد الفعلي للأقساط يأتي من Ledger، وتقدم الأقساط مشتق/متصالح معه.
5. Revision جديدة لخطة الأقساط تحتفظ بالخطة السابقة كـ`SUPERSEDED` ولا تمحو التاريخ.
6. المتابعة الذكية والمنبّه القوي أدوات جدولة فقط؛ لا تغير الرصيد ولا تنشئ دفعة.
7. Exact Alarm اختياري ولا يجوز جعله نقطة فشل وحيدة؛ WorkManager يبقى fallback.
8. كل دين لشخص موجود يبقى حسابًا مستقلًا بمعرف مستقل.
9. الأموال لا تستخدم Floating Point.
10. لا destructive migrations.
11. لا أسرار أو مفاتيح توقيع داخل المستودع.
12. لا دمج إلى `main` دون طلب صريح من صاحب المشروع.

## قاعدة البيانات الحالية — Schema v6

الجداول الأساسية الحالية تشمل:

- `persons`
- `debts`
- `ledger_entries`
- `reminders`
- `audit_events`
- `document_identities`
- `issued_documents`
- `payment_promises`
- جداول خطط الأقساط والأقساط المضافة في v6.

Migrations:

- v1 → v2: reminders.
- v2 → v3: audit events.
- v3 → v4: document identities + issued documents.
- v4 → v5: payment promises + indexes.
- v5 → v6: installment plans/installments والبنية اللازمة لتاريخ الخطط والمتابعة.

اختبارات Baseline تتحقق من انتقال قواعد v1 وv2 وv3 حتى v6 دون فقد البيانات، ومن إنشاء جداول الأقساط بصورة صحيحة.

## التحقق الحالي

**Android CI #281** — Run `32849476379` — على الرأس `c92857c` نجح بالكامل:

- Unit tests ✅
- Lint ✅
- Debug APK ✅
- Room Schema v6 export/current check ✅
- Android Emulator instrumentation: **53/53** ✅
- 0 failed ✅
- `BUILD SUCCESSFUL in 3m 37s` لاختبارات Android ✅
- اختبار Regression يثبت بقاء نافذة وَصل مفتوحة بعد تفعيل المنبّه القوي مع ظهور تحذير Exact Alarm وزر الإذن وحفظ التعديل ✅
- استخراج إيصال PDF من ملفات التطبيق ✅
- الإيصال متعدد الصفحات (صفحتان على الأقل) ✅
- `PAY-2026-00042` ✅
- `AL NOOR TRADING` ✅
- `123,456.78 USD` ✅
- تحويل صفحات PDF إلى PNG كدليل بصري آلي ✅

Artifacts من #281:

- `Wasl-debug` — Artifact `9563818121` — SHA-256 `9c73806206d90ac7dd57e6127030bb1c057aa1d728c97ee09df88c42fc00420f`.
- `Wasl-room-schema` — Artifact `9563818656` — SHA-256 `2a829f60b3f4a2be636fe6a73efdf83d5efcaf2f8746d5346c7bfe00eb31872e`.
- `Wasl-payment-receipt-evidence` — Artifact `9564031722` — SHA-256 `e6f3aee1810d4256cce18b57ab34ee544ea347a8bc31f47fd329d38d670d5e0e`.
- `Wasl-room-instrumentation-results` — Artifact `9564032564` — SHA-256 `31cef7959719a78429246195628ef9889974a9d934afa2df2c97e57b28382076`.

## ديون تقنية غير حاجبة

- نقل Compose tests من `createComposeRule` v1 إلى API v2 لاحقًا بعد ضبط التزامن المطلوب.
- ترقية `actions/setup-java@v4` إلى v5 وتحديث Actions التي ما زالت تصدر تحذيرات Node 20/Node 24 compatibility.
- إزالة safe calls غير الضرورية في `StrongAlarmStoreInstrumentedTest`.
- تحسين زمن CI عبر Gradle configuration cache متى كان ذلك آمنًا.
- بعض رسائل Emulator مثل initial adb offline/ColorBuffer ظهرت في CI لكنها لم تؤثر على نجاح 53/53.

## مرشحات المراحل التالية

تُختار المرحلة التالية من `WASL_MASTER_PROJECT_PROMPT.md` و`SPEC.md` دون إسقاط أي قرار سابق. من المرشحات الموجودة في الخطة التأسيسية:

- التذكيرات المتكررة العامة خارج سياق الاستحقاق.
- قفل التطبيق/الحماية المحلية والقياسات الحيوية إن اعتمدت.
- Backup/Restore مشفر.
- أنواع مستندات مالية إضافية.
- بحث متقدم داخل العمليات والمستندات والمبالغ والتواريخ.
- اختبار بصري وإتاحة موسع على أحجام نوافذ وFont Scale وTalkBack.
- تجهيز Release signing للإنتاج عندما يحين وقت الإصدار.
